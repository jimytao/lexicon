# Lexicon v0.6.4 补丁版（Patch Release）

这是基于 v0.6.3 的补丁版本，主要修复输入框键盘遮挡、AI Model 选择体验，以及 AI 缓存 / 历史记录之间的状态一致性问题。

## 本次补丁重点

- 修复 iOS / Android 虚拟键盘弹出后输入框可能被遮挡的问题。
- 优化 Settings 里的 AI Model 输入框，支持模糊匹配、相关度排序和实时重排。
- 修复历史记录 AI 缓存星标不可靠的问题。
- 明确区分“真实缓存”和“历史 AI 意图”：
  - 亮色星标：当前确实存在 AI 缓存。
  - 暗色星标：以前进行过 AI 查询，但缓存已被清除。
- 点击历史记录时，优先恢复真实缓存；没有缓存时才按历史 AI 类型重新生成。
- 普通输入 / suggestion 点击时，只要存在真实 AI 缓存，即使历史记录已删除，也可以直接恢复缓存。
- 普通输入不会因为暗色星标或历史 `aiMode` 自动重跑旧 AI 类型。
- 修复输入框右侧强制 AI 按钮未正确写入新历史项的问题。
- 修复 `Clear Cache` 按钮漏判短语 / 句子缓存的问题。
- 修复清空缓存后当前页面仍显示旧 AI 结果的状态错位问题。

## 详细改动

### 1. 键盘遮挡补丁

- 增强输入框 focus 后的自动滚动稳定性。
- 使用 `visualViewport` 辅助计算有效键盘高度。
- 为底部 padding 增加上下限，降低 Android WebView 双重 resize / pan 导致的过度补偿风险。
- Android 原生键盘配置保持不变，本次只做低风险 JS 侧修正。
- 如果未来仍有个别机型抖动，可继续尝试滚动节流方案。

### 2. AI Model 模糊选择补丁

- 对模型 ID 增加 fuzzy scoring。
- 优先匹配：
  - 完全匹配
  - 前缀匹配
  - 子串匹配
  - 顺序字符匹配
- 同分时保留原始顺序，避免列表跳动过大。
- 输入和重新聚焦时，模型列表会实时展开并按相关度重排。

### 3. AI 缓存 / 历史记录补丁

- 历史记录星标现在区分真实缓存与历史 AI 意图。
- 点击历史记录时，缓存恢复优先级为：
  1. `aiFullCache`
  2. `phraseCache`
  3. `aiCache`
- 如果没有真实缓存，但历史记录有 AI 类型，则按历史类型重新生成。
- 普通输入只要存在真实缓存就恢复，不再要求历史记录存在。
- 普通输入在无缓存时不会继承历史 `aiMode`，仍按当前模式或用户点击的按钮执行。

### 4. 数据管理补丁

- `Clear History` 和 `Clear Cache` 继续保持分离。
- 清空历史不会删除 AI 缓存，缓存仍可通过普通输入或 suggestion 恢复。
- 清空缓存会保留历史记录中的 AI 意图，因此历史项会从亮星变为暗星。
- 清空缓存时会同步清空当前页面已加载的 AI 结果，避免 UI 显示旧结果。
- `Clear Cache` 按钮现在会同时检查 `aiCache`、`aiFullCache`、`phraseCache`。

## 构建产物

- Android APK
- Windows EXE 安装包
- Windows MSI 安装包


node_modules
dist

# Internal docs — now tracked in repo for version history
# (keystore and signing configs are still excluded below)

# Android keystore (never commit — back up locally)
*.keystore

# Tauri
src-tauri/target
src-tauri/*.key
src-tauri/*.key.pub

# Android
android/.gradle
android/app/build
android/build
*.keystore
*.jks

# Release bundles
release-artifacts/


# Claude Code
.claude/

# IDE
.idea
*.iml

# OS
.DS_Store
Thumbs.db

# SQLite WAL/SHM temp files
*.db-shm
*.db-wal

# Playwright MCP temp files
.playwright-mcp/

# Temp
app-icon.png

# Local Secrets
.env.release

# Release Binaries
*.apk
*.exe
*.msi
*.sig

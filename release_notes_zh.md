落地移动端原生 SQLite 词库并修复查询逻辑、编译与发版配置问题
1. 新增：移动端落地 @capacitor-community/sqlite 原生词库，提供极速本地查询并自动双向 fallback 至 sql.js。
2. 修复：重构通用执行器以返回标准对象数组，彻底修复原生端因 JSON 字典键无序而引发的字段错位与释义显示空白逻辑漏洞。
3. 修复：修改 Package.swift 将 Windows 反斜杠改为 Unix 正斜杠，彻底解决 iOS 在 macOS 环境下的 Swift SPM 编译失败问题。
4. 修复：修复 iOS 独特的表头行过滤逻辑在 Native 端不可达的分支死锁缺陷。
5. 优化：优化冷启动检查，仅在主词库缺失时执行资源拷贝，解决未打包可选英英词库时每次冷启动都重新灌入 80MB 数据导致的卡顿。
6. 优化：更新 MDX 转换脚本，在编译期递归解析并录入 @@@LINK 重定向词条，使应用能完美联想和检索复数、时态等派生变形词。
